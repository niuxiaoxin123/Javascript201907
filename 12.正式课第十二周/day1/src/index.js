// index.js ： 相当于webpack的打包的入口；相当于vue项目中的main.js
// ReactDOM.render(<App/>,document.getElementById("root"));
// root 是public中index.html 的一个元素；
// app.js : 相当于app.vue

// create-react-app : react的脚手架；会自动生成一个react的项目；src文件放的资源

// yarn start : 启动该项目；会默认启动一个3000端口的服务，是webpack-development-server;会自动的更新；

// yarn build : 将项目进行打包，一般在项目上线之前进行打包；会自动在项目下生成一个build文件，里面有打包之后index.html 和js;

// yarn  eject : 将所有的配置文件暴露出来，但是这一步是不可逆的；如果需要修改配置才执行这样的操作；


// react： 构建用户界面的JavaScript库

// 虚拟的DOM：VUE 和react;先操作虚拟的DOM，然后再操作真实的DOM；
//
// jquery : 真实的DOM；

//.js 的后缀名可以省略；；
// webpack是以当前index.js为入口；
import "./pages/6.JSX-self";

